# Settings visual checkpoint

Settings is already bounded as unavailable. The previous screen claimed profile loading/saving, avatar upload, notification preferences, privacy controls, account deletion, 2FA activation, active sessions, login history, wallet addresses, auto-staking, and transaction notifications. The referenced routes are not verified for these operations, so controls and success states were removed.

The page documents authenticated ownership, field validation, concurrency protection, durable preference storage, privacy/consent, secure uploads, MFA enrollment/recovery, session/login evidence, destructive-workflow confirmation, wallet-provider boundaries, transaction-preference semantics, audit logging, and clear success/failure feedback. Files: `settings-desktop.webp`, `settings-mobile.png`.
