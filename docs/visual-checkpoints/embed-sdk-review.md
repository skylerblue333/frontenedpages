# EmbedSDK visual checkpoint

EmbedSDK was upgraded from a screen that claimed a beta SDK, exposed package and iframe examples, showed wallet, AI, payment, chart, social, tip, and NFT widgets, included a payment webhook example, offered API-key and sales actions, and published pricing, limits, SLA, and revenue claims. No verified package, contract, credential, custody, provider, or deployment evidence supported those claims, so they were removed.

The registered route is `/embed-s-d-k`; `/embed-sdk` returns the application 404 and is not the checkpoint route.

No SDK version, package, install command, import, iframe URL, widget, API key, secret, wallet balance, transaction, signing action, AI chat, AI output, payment, token, NFT, social feed, price chart, tip, webhook, endpoint, pricing plan, API limit, SLA, analytics, revenue share, user outcome, sign-in, code copy, credential generation, provider connection, API request, database read or write, transaction, payment, webhook registration, or production deployment is read, calculated, displayed, asserted, initiated, or simulated.

Files: `embed-sdk-desktop.webp`, `embed-sdk-mobile.png`.

The 390px capture was reviewed for readable wrapped unavailable disclosure, stacked embed-integration readiness cards, stacked wallet/AI/payment/widget surfaces, developer/crypto/AI/payment/secret-safety warn-and-proceed notice, safe API/security/crypto/availability links, and absence of horizontal overflow.
