import { AlertTriangle, BarChart3, CheckCircle2, Droplets, FileWarning, LockKeyhole, ShieldAlert, WalletCards } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const readiness = [
  { label: "Verified chain, exchange, pool, pair, and token identity", value: "Not connected", icon: Droplets },
  { label: "Order-book, price, volume, liquidity, spread, and chart data", value: "Unavailable", icon: BarChart3 },
  { label: "Wallet, quote, approval, order, swap, and settlement controls", value: "Not configured", icon: WalletCards },
  { label: "Timestamping, provenance, manipulation checks, and risk disclosure", value: "Not verified", icon: LockKeyhole },
];

const boundaries = [
  { title: "No market-data claim", description: "No pair, token, price, percentage change, order book, bid, ask, spread, volume, liquidity, TVL, chart, candle, pool, or exchange-rate value is read, calculated, displayed, or simulated.", icon: BarChart3 },
  { title: "No trading or liquidity action", description: "No wallet connection, quote, approval, order, swap, liquidity deposit, withdrawal, signature, transaction, refresh request, API call, or settlement can be initiated here.", icon: WalletCards },
  { title: "No analytics or performance claim", description: "No depth analysis, trend, forecast, signal, AI insight, execution quality, uptime, latency, throughput, slippage, fee, or investment recommendation is asserted.", icon: FileWarning },
  { title: "Crypto warn-and-proceed", description: "DEX data can be delayed, incomplete, manipulated, or chain-specific, and trades can cause irreversible loss. Verify the network, contract, pool, quote, fees, and slippage and obtain qualified advice before acting.", icon: AlertTriangle },
];

export default function DEXDepthChart() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Droplets} title="DEX Depth Chart" subtitle="DEX-readiness status; no live pairs, order books, prices, liquidity, pools, charts, quotes, wallet connections, or trading actions are available in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">DEX market data is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen contained hardcoded token pairs, fabricated prices, percentage changes, volume, TVL, spread, random order-book depth, random OHLCV charts, pool analytics, a refresh control, pair selection, tabs, and clickable pool/order-book interactions. Those values were not sourced, timestamped, or independently verified, so the data and controls were removed rather than presented as live market information.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Droplets aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">DEX-readiness status</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">A trustworthy DEX depth surface requires verified network and contract identity, authoritative indexer or RPC data, timestamps and provenance, pool and pair reconciliation, manipulation and stale-data checks, quote and slippage semantics, wallet and approval security, transaction verification, failure handling, and clear risk disclosure. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{readiness.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="dex-boundaries-heading"><h2 id="dex-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2">{boundaries.map(({ title, description, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><Icon aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Card>)}</div></section><div className="flex flex-wrap gap-3"><Link href="/crypto-hub"><Button variant="outline">View crypto status</Button></Link><Link href="/crypto-research-hub"><Button variant="outline">View research status</Button></Link><Link href="/contact-us-form"><Button variant="outline">Ask about availability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No pair, token, price, percentage change, order book, bid, ask, spread, volume, liquidity, TVL, chart, candle, pool, exchange rate, depth analysis, forecast, signal, AI insight, quote, wallet connection, approval, order, swap, liquidity deposit, withdrawal, signature, transaction, refresh, API request, database read or write, fee, slippage, or investment recommendation is performed. This page is not evidence of current market conditions, liquidity, quote accuracy, trade execution, or financial performance.</p></div></Card></main></div>;
}
