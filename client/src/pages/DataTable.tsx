import { CheckCircle2, Database, Filter, Grid2X2, LockKeyhole, Search, Settings2, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const readiness = [
  { label: "Authorized source, schema, rows, columns, and provenance", value: "Not connected", icon: Grid2X2 },
  { label: "Search, filters, sorting, pagination, refresh, and query semantics", value: "Unavailable", icon: Filter },
  { label: "Selection, editing, validation, persistence, and conflict handling", value: "Not configured", icon: Settings2 },
  { label: "Privacy, permissions, export, audit, and failure handling", value: "Not verified", icon: LockKeyhole },
];

const boundaries = [
  { title: "No table-data claim", description: "No row, column, record, value, identifier, timestamp, count, query, schema, sort order, filter result, or database state is read, displayed, calculated, or simulated.", icon: Database },
  { title: "No table action or persistence claim", description: "No activate, deactivate, search, filter, sort, paginate, select, edit, create, save, delete, refresh, export, API request, database read or write, or account mutation can be initiated here.", icon: Settings2 },
  { title: "No authorization or privacy claim", description: "No owner, role, permission, data subject, consent, retention, redaction, access decision, audit record, or external data integration is available. Do not enter sensitive information here.", icon: LockKeyhole },
  { title: "No operational or business claim", description: "No customer, user, revenue, transaction, balance, portfolio, KPI, analytics, AI, performance, or business result is represented by this page.", icon: Search },
];

export default function DataTable() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Grid2X2} title="Data Table" subtitle="Table-readiness status; no rows, records, schema, filters, sorting, pagination, editing, exports, permissions, or database state are available in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Data table is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen contained generic content and a non-functional Activate/Deactivate toggle, implying that a data table was connected. No verified source, schema, rows, columns, permissions, persistence, or database integration was connected, so the generic control was removed and the state is made explicit.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Grid2X2 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Table-readiness status</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">Production tables require an authorized source and schema, stable row identity, query and pagination contracts, filter and sort semantics, validation and editing safeguards, concurrency handling, privacy, access control, exports, observability, and auditable persistence. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{readiness.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="table-boundaries-heading"><h2 id="table-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2">{boundaries.map(({ title, description, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><Icon aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Card>)}</div></section><div className="flex flex-wrap gap-3"><Link href="/data-grid"><Button variant="outline">View grid status</Button></Link><Link href="/data-export"><Button variant="outline">View export status</Button></Link><Link href="/contact-us-form"><Button variant="outline">Ask about availability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No row, column, record, value, identifier, timestamp, count, query, schema, sort, filter, selection, edit, create, save, delete, refresh, export, API request, database read or write, customer, user, revenue, transaction, balance, portfolio, KPI, analytics, AI, performance, or business result is performed. This page is not evidence of table connectivity, data accuracy, authorization, privacy compliance, persistence, or operational performance.</p></div></Card></main></div>;
}
