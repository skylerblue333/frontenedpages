import { Globe2, Code2, FileCheck2, KeyRound, LockKeyhole, Network, ShieldAlert, WalletCards } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const evidence = [
  { label: "Extension package, browsers, versions, and release channel", value: "Unavailable", icon: Globe2 },
  { label: "Wallet, account, key, permission, and page-access model", value: "Not configured", icon: WalletCards },
  { label: "AI, privacy, telemetry, storage, and network behavior", value: "Not connected", icon: Network },
  { label: "Security review, source, beta enrollment, and audit", value: "Disabled", icon: FileCheck2 },
];

export default function BrowserExtension() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Globe2} title="Browser Extension" subtitle="Browser-extension services are not connected in this deployment. No extension package, installation, permission, wallet, AI, privacy, monitoring, or beta-enrollment result is being reported or created." /><main className="mx-auto max-w-5xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Extension actions are unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen presented ShadowChat as an in-development Web3 browser extension and claimed wallet overlays, one-click tipping and purchases, HopeAI overlays, tracker blocking and relay routing, AI-ranked search, price and whale alerts, smart-contract simulation, a password vault, silent monitoring of every page, zero data collection, local AI, end-to-end encryption, open-source status, an audit, browser-store installation, and 500 SKY444 plus a founding badge for beta testers. No signed package, browser-store listing, permission manifest, wallet and key model, content or telemetry policy, relay service, AI runtime, extension security review, source repository, audit evidence, enrollment service, or token ledger was connected. Those claims, browser buttons, privacy guarantees, and Join Beta action were removed rather than implying installation, security, financial access, or data protection.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Globe2 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Extension readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production browser extension requires signed and versioned packages, narrowly scoped permissions, browser-store review, a threat model, secure update and rollback controls, explicit page-access and telemetry disclosures, private-key isolation, wallet consent, content and credential handling, AI data-flow documentation, accessibility, incident response, independent security review, and auditable enrollment. None are available through this route.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link href="/wallet"><Button>View wallet status</Button></Link><Link href="/privacy"><Button variant="outline">View privacy status</Button></Link><Link href="/security"><Button variant="ghost">View security status</Button></Link></div></Card><section aria-labelledby="extension-evidence-heading"><h2 id="extension-evidence-heading" className="mb-4 text-xl font-semibold">Current extension evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not install unverified extensions or enter passwords, access tokens, private keys, seed phrases, wallet credentials, payment details, confidential page content, or sensitive personal information here. An unavailable extension page is not evidence that browsing data is private, credentials are protected, a wallet is secure, or a beta reward exists.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><KeyRound aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No package download, browser installation, permission request, wallet connection, balance read, page-content access, history read, tracker block, relay request, AI call, price alert, credential storage, transaction simulation, telemetry event, beta enrollment, notification, or external extension API call is read, written, sent, stored, or simulated by this page.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Code2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No browser count, release date, feature status, data-collection guarantee, encryption claim, audit result, installation state, wallet balance, protection result, beta participant, token amount, or security conclusion is fabricated as a fallback. Verify future extension records through browser stores, published source, independent audits, and trusted server-side systems.</p></div></Card></main></div>;
}
