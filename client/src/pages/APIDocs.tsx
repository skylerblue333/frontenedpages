import { AlertTriangle, BookOpen, Code2, Database, ExternalLink, KeyRound, LockKeyhole, Network, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const states = [
  { label: "API base URL, routes, and schemas", value: "Not verified", icon: Network },
  { label: "Authentication, keys, and scopes", value: "Unavailable", icon: KeyRound },
  { label: "Rate limits, quotas, and billing", value: "Not configured", icon: ShieldCheck },
  { label: "SDKs, webhooks, and operational evidence", value: "Unavailable", icon: Code2 },
];

export default function APIDocs() {
  return <div className="min-h-screen bg-background"><PageHeader icon={BookOpen} title="API Documentation" subtitle="No verified public REST, GraphQL, webhook, SDK, authentication, rate-limit, or API-key service is connected in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><AlertTriangle aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">API documentation is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous page presented a Generate API Key action, an api.skycoin4444.com base URL, feedback, roadmap, agent-debate, and order-creation endpoints, Bearer/API-key/OAuth examples, fixed Free and Pro rate tiers, unlimited custom limits, GraphQL/webhook references, SDKs, and a Postman collection. No verified public API registry, deployment, route contract, authentication service, key lifecycle, authorization scope, rate-limit policy, billing entitlement, SDK package, webhook receiver, or operational evidence was connected. Those claims and inert controls were removed.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><BookOpen aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">API publication readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production API reference requires a verified deployment and versioned contract, route and schema ownership, authentication and authorization semantics, input/output validation, error and retry behavior, idempotency, rate and cost limits, secret handling, backward compatibility, security review, observability, lifecycle and deprecation policy, and executable examples that point to real services. None are available through this route. No key is generated and no request should be sent to the displayed placeholder domain.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Card className="border border-primary/30 bg-background/80 p-4"><Network aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><h3 className="font-semibold">No route claim</h3><p className="mt-1 text-sm text-muted-foreground">No endpoint, method, schema, base URL, response, version, uptime, latency, or availability is documented as live.</p></Card><Card className="border border-primary/30 bg-background/80 p-4"><KeyRound aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><h3 className="font-semibold">No credential claim</h3><p className="mt-1 text-sm text-muted-foreground">No API key, token, OAuth scope, account entitlement, or key-rotation workflow is created or shown.</p></Card><Card className="border border-primary/30 bg-background/80 p-4"><ShieldCheck aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><h3 className="font-semibold">No rate-limit claim</h3><p className="mt-1 text-sm text-muted-foreground">No quota, tier, billing plan, unlimited access, request allowance, or cost outcome is represented.</p></Card><Card className="border border-primary/30 bg-background/80 p-4"><Code2 aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><h3 className="font-semibold">No SDK claim</h3><p className="mt-1 text-sm text-muted-foreground">No SDK, GraphQL schema, webhook guide, Postman collection, package, or executable integration is available.</p></Card></div><div className="mt-6 flex flex-wrap gap-4"><Link href="/api-status"><Button>View API status</Button></Link><Link href="/api-management"><Button variant="outline">View API management</Button></Link><Link href="/contact-us-form"><Button variant="ghost">Ask about API access</Button></Link></div></Card><section aria-labelledby="api-state-heading"><h2 id="api-state-heading" className="mb-4 text-xl font-semibold">Current API evidence</h2><div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-4">{states.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not enter passwords, access tokens, private keys, seed phrases, database credentials, or production secrets here. Do not send requests to an unverified endpoint or assume that an example credential, API key action, SDK label, or rate tier is real.</p></div></Card></main></div>;
}
