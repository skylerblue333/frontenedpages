import { CheckCircle2, Download, FileOutput, LockKeyhole, Search, Settings2, ShieldAlert, Table2 } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const readiness = [
  { label: "Authenticated owner, export scope, records, and authorization", value: "Not connected", icon: LockKeyhole },
  { label: "Source data, filters, field definitions, and export provenance", value: "Unavailable", icon: Table2 },
  { label: "CSV, JSON, spreadsheet, PDF, archive, download, and delivery", value: "Not configured", icon: Download },
  { label: "Privacy, redaction, retention, audit, and failure handling", value: "Not verified", icon: Settings2 },
];

const boundaries = [
  { title: "No data or file claim", description: "No record, table, report, field, filter, metric, file, row count, export format, download, timestamp, checksum, or generated artifact is read, calculated, displayed, created, or simulated.", icon: FileOutput },
  { title: "No export action or persistence claim", description: "No sign-in, create, search, configure, select, export, download, share, email, schedule, delete, API request, database read or write, or account mutation can be initiated here.", icon: Download },
  { title: "No privacy or authorization claim", description: "No data subject, role, permission, ownership, consent, redaction, retention, deletion, audit record, access decision, or compliance outcome is available. Do not enter sensitive data here.", icon: LockKeyhole },
  { title: "No business or financial claim", description: "No customer, user, revenue, transaction, balance, portfolio, tax, accounting, operational, AI, analytics, or financial result is exported or asserted.", icon: Table2 },
];

export default function DataExport() {
  return <div className="min-h-screen bg-background"><PageHeader icon={FileOutput} title="Data Export" subtitle="Export-readiness status; no records, reports, files, formats, downloads, delivery jobs, permissions, or account data are available in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Data export is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen showed a fake authentication gate, non-functional Sign In, New, Search, and Settings controls, a simulated loading state, a multi-format export implication, and a generic prompt implying an export item could be created. No verified source data, export scope, permissions, file generator, download, delivery, privacy, or audit integration was connected, so those claims and controls were removed.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><FileOutput aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Export-readiness status</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">Production exports require authenticated ownership, scoped queries, data contracts, field and filter semantics, reproducible files, format validation, access control, redaction and privacy review, retention and deletion, secure delivery, auditability, and failure handling. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{readiness.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="export-boundaries-heading"><h2 id="export-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2">{boundaries.map(({ title, description, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><Icon aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Card>)}</div></section><div className="flex flex-wrap gap-3"><Link href="/custom-reports"><Button variant="outline">View report status</Button></Link><Link href="/data-privacy"><Button variant="outline">View privacy status</Button></Link><Link href="/contact-us-form"><Button variant="outline">Ask about availability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No record, table, report, field, filter, metric, file, row count, export format, download, timestamp, checksum, artifact, sign-in, create, search, configure, select, export, share, email, schedule, delete, API request, database read or write, personal data, customer, user, revenue, transaction, balance, portfolio, tax, accounting, operational, AI, analytics, or financial result is performed. This page is not evidence of export access, file integrity, privacy compliance, authorization, or data accuracy.</p></div></Card></main></div>;
}
