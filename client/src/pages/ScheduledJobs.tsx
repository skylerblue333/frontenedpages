import { AlertTriangle, CheckCircle2, Clock3, FileCheck2, KeyRound, LockKeyhole, Network, PlayCircle, ShieldCheck, Terminal, Workflow } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const surfaces = [
  ["Job ownership and trigger scope", "Authenticated owner, tenant, purpose, schedule or event trigger, timezone, retry policy, and authorization", "Not connected", Workflow],
  ["Execution and idempotency evidence", "Run identifier, start/end time, status, concurrency control, idempotency key, timeout, and durable result", "Unavailable", PlayCircle],
  ["Connector and data boundaries", "External service scope, server-side secrets, payload validation, least privilege, rate limits, and data minimization", "Not verified", Network],
  ["Monitoring, failure, and recovery", "Structured logs, alerts, backoff, dead-letter handling, audit history, pause/resume, rollback, and incident response", "Not configured", ShieldCheck],
] as const;

export default function ScheduledJobs() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Clock3} title="Scheduled Jobs" subtitle="Automation-readiness status; no verified scheduler, job registry, trigger source, background worker, connector, secret store, execution log, or durable result backend is connected in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-amber-400/30 bg-amber-950/20 p-6"><div className="flex items-start gap-3"><AlertTriangle aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-amber-300" /><div><h2 className="font-semibold text-amber-100">Scheduled jobs are unavailable</h2><p className="mt-1 text-sm leading-6 text-amber-100/80">The previous screen exposed nonfunctional Sign In, New, Search, Settings, loading, and empty-data controls without a scheduler, worker, trigger source, or execution store. Those controls were removed. No job, schedule, trigger, execution, connector, secret, result, failure, user, tenant, or availability state is displayed, calculated, stored, transmitted, verified, granted, executed, or mutated from this page.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Clock3 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Automation-readiness boundary</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">A trustworthy scheduled-job system requires authenticated ownership and scope, explicit purpose, trigger and timezone, bounded frequency, validated inputs, least-privilege connector scopes, server-side secrets, idempotency and concurrency controls, run identifiers and timestamps, durable results, timeout and retry policy, structured logs, failure and dead-letter handling, alerting, pause/resume, rollback, auditability, privacy, and incident response. A schedule, execution, result, failure rate, connector action, or background guarantee is not a fact without verified records. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{surfaces.map(([label,,value,Icon])=><Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="scheduled-jobs-surfaces-heading"><h2 id="scheduled-jobs-surfaces-heading" className="mb-4 text-xl font-semibold">Job control surfaces</h2><div className="grid gap-4 md:grid-cols-2">{surfaces.map(([title,scope,status,Icon])=><Card key={title} className="border border-border/50 bg-card p-6"><div className="flex items-start justify-between gap-4"><Icon aria-hidden="true" className="h-7 w-7 shrink-0 text-primary" /><span className="rounded-full border border-border/60 px-2 py-1 text-xs text-muted-foreground">{status}</span></div><h3 className="mt-4 text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{scope}. No job, schedule, trigger, execution, connector, secret, result, failure, user, tenant, privacy, compliance, or production status is asserted.</p></Card>)}</div></section><section aria-labelledby="scheduled-jobs-boundaries-heading"><h2 id="scheduled-jobs-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2"><Card className="border border-border/50 bg-card p-6"><CheckCircle2 aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">No automation operation</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">No job creation, schedule registration, trigger subscription, connector call, secret entry, worker start, execution, retry, pause, resume, result write, database read or write, export, or deletion is performed.</p></Card><Card className="border border-border/50 bg-card p-6"><AlertTriangle aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">Personal data, identity, AI, finance, crypto, privacy, safety, security, compliance, and authorization warn-and-proceed</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Do not enter passwords, authentication codes, API tokens, private keys, seed phrases, wallet addresses, identity documents, health records, payment details, account numbers, or confidential source code here. Do not schedule an unreviewed action or treat this page as evidence of execution, delivery, connector authorization, security, privacy protection, compliance, or legal conclusion. Verify owner, scope, trigger, timezone, payload, secrets, idempotency, retries, rate limits, recipient, privacy, security, authorization, and human approval before enabling automation.</p></Card></div></section><div className="flex flex-wrap gap-3"><Link href="/scheduled-reports"><Button variant="outline"><FileCheck2 aria-hidden="true" className="mr-2 h-4 w-4" />Review scheduled reports</Button></Link><Link href="/permissions"><Button variant="outline"><KeyRound aria-hidden="true" className="mr-2 h-4 w-4" />Review permissions</Button></Link><Link href="/security"><Button variant="outline"><ShieldCheck aria-hidden="true" className="mr-2 h-4 w-4" />Review security</Button></Link><Link href="/privacy-center"><Button variant="outline"><LockKeyhole aria-hidden="true" className="mr-2 h-4 w-4" />Review privacy</Button></Link><Link href="/documentation"><Button variant="outline"><FileCheck2 aria-hidden="true" className="mr-2 h-4 w-4" />Review documentation</Button></Link><Link href="/system-observability"><Button variant="outline"><Terminal aria-hidden="true" className="mr-2 h-4 w-4" />Review observability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><p className="text-sm leading-6 text-muted-foreground">No job creation, schedule registration, trigger subscription, connector call, secret entry, worker start, execution, retry, pause, resume, result write, database read or write, export, or deletion is performed. This page is not evidence of execution, delivery, connector authorization, security, privacy protection, compliance, or legal conclusion.</p></Card></main></div>;
}
