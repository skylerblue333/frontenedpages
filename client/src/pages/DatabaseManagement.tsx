import { Activity, CheckCircle2, Database, FileCog, LockKeyhole, RefreshCw, Server, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const readiness = [
  { label: "Database provider, connection, schemas, migrations, and ownership", value: "Not verified", icon: Database },
  { label: "Queries, transactions, indexes, constraints, performance, and health", value: "Unavailable", icon: Activity },
  { label: "Backups, restores, replication, recovery, and environment parity", value: "Not configured", icon: RefreshCw },
  { label: "Credentials, secrets, permissions, audit, and incident handling", value: "Not connected", icon: LockKeyhole },
];

const boundaries = [
  { title: "No database health or metric claim", description: "No connection, schema, migration, table, row count, active user, transaction count, success rate, response time, uptime, query result, or operational metric is read, calculated, displayed, or simulated.", icon: Activity },
  { title: "No management action", description: "No get-started, learn-more, documentation, create, alter, migrate, query, refresh, backup, restore, replicate, delete, API request, database read or write, or credential mutation can be initiated here.", icon: Server },
  { title: "No security or permission claim", description: "No database credential, secret, role, privilege, access decision, encryption, network boundary, audit record, vendor control, or incident response is verified. Never enter credentials here.", icon: LockKeyhole },
  { title: "No integration or business claim", description: "No analytics, automation, customer, user, financial, transaction, performance, or external integration result is asserted by this page.", icon: FileCog },
];

export default function DatabaseManagement() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Database} title="Database Management" subtitle="Database-readiness status; no connection, schema, migration, query, health, backup, permission, credential, or operational state is available in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Database management is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen claimed a fully functional database with live data and real-time updates, presented generic feature cards and non-functional Get Started, Learn More, and Documentation buttons, and displayed invented active-user, transaction, success-rate, and response-time metrics. No verified database provider, connection, schema, migration, permission, backup, or observability integration was connected, so those claims and controls were removed.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Database aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Database-readiness status</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">Production database operations require an identified provider and environment, secure server-side connections, reviewed schema and migrations, constraints and indexes, transaction and concurrency handling, backup and recovery testing, environment parity, secret management, least-privilege access, auditability, observability, and incident response. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{readiness.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="database-boundaries-heading"><h2 id="database-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2">{boundaries.map(({ title, description, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><Icon aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Card>)}</div></section><div className="flex flex-wrap gap-3"><Link href="/data-lake"><Button variant="outline">View data lake status</Button></Link><Link href="/system-status"><Button variant="outline">View system status</Button></Link><Link href="/contact-us-form"><Button variant="outline">Ask about availability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No connection, schema, migration, table, row count, active user, transaction count, success rate, response time, uptime, query result, get-started action, documentation link, create, alter, migrate, query, refresh, backup, restore, replicate, delete, API request, database read or write, credential, secret, role, privilege, encryption guarantee, analytics, automation, customer, financial, performance, or integration result is performed. This page is not evidence of database health, security, availability, or data accuracy.</p></div></Card></main></div>;
}
