import { Activity, Bot, Database, FileClock, LockKeyhole, Network, ShieldAlert, Workflow } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const evidence = [
  { label: "Workflow definitions and triggers", value: "Unavailable", icon: Workflow },
  { label: "Jobs, schedules, queues, and retries", value: "Not configured", icon: FileClock },
  { label: "Integrations, credentials, and permissions", value: "Not connected", icon: Network },
  { label: "Execution, outcomes, and observability", value: "Disabled", icon: Activity },
];

export default function AutomationEngine() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Workflow} title="Automation Engine" subtitle="Automation services are not connected in this deployment. No workflow, trigger, job, schedule, feature flag, rate limit, kill-switch, integration, or execution outcome is being reported or changed." /><main className="mx-auto max-w-5xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Automation control is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen exposed fabricated active workflows, run counts, payment confirmation, reward distribution, fraud escalation, simulation ticks, AI and wallet jobs, feature toggles, rate-limit values, and a kill-switch that claimed to freeze payments and pause AI. No scheduler, queue, worker, provider, credential store, authorization model, execution ledger, or emergency-control contract was connected. Those controls and claims were removed rather than implying that any task ran, payment changed, wallet synchronized, AI acted, or system was placed in a safe state.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Workflow aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Automation readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production automation engine requires typed workflow definitions, authenticated ownership, trigger validation, idempotency, durable queues, worker isolation, credential scoping, rate limits, retry and dead-letter semantics, approval gates, side-effect confirmation, cancellation, kill-switch authority, audit events, monitoring, and independently verifiable outcomes. None are available through this route.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link href="/automation-workflows"><Button>View workflow status</Button></Link><Link href="/automation-rules"><Button variant="outline">View rule status</Button></Link><Link href="/security"><Button variant="ghost">View security status</Button></Link></div></Card><section aria-labelledby="automation-evidence-heading"><h2 id="automation-evidence-heading" className="mb-4 text-xl font-semibold">Current automation evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not enter passwords, access tokens, private keys, seed phrases, API credentials, payment data, wallet information, confidential workflows, or sensitive personal information here. An unavailable automation page is not evidence that an action ran, was safe, was authorized, or was reversible.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Database aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No workflow query, job enqueue, scheduler action, feature-flag change, rate-limit change, kill-switch action, payment, reward, wallet transfer, AI call, notification, webhook, or external integration call is read, written, sent, stored, or simulated by this page.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Bot aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No workflow count, run count, job progress, ETA, status, feature state, current request rate, limit, uptime, success rate, failure, payment result, reward, fraud decision, wallet balance, AI action, or safety conclusion is fabricated as a fallback. Verify future automation records through independently trusted server-side systems.</p></div></Card></main></div>;
}
