import { AudioLines, Download, FileAudio, FolderOpen, LockKeyhole, ShieldAlert, SlidersHorizontal } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const evidence = [
  { label: "Audio assets, projects, and ownership", value: "Unavailable", icon: FolderOpen },
  { label: "Editing, effects, and processing", value: "Not configured", icon: SlidersHorizontal },
  { label: "Render, export, and delivery", value: "Disabled", icon: Download },
  { label: "Rights, licensing, and audit", value: "Not connected", icon: ShieldAlert },
];

export default function AudioEditing() {
  return <div className="min-h-screen bg-background"><PageHeader icon={AudioLines} title="Audio Editing" subtitle="Audio editing and media-processing services are not connected in this deployment. No audio asset, project, edit, render, export, license, or delivery result is being reported or created." /><main className="mx-auto max-w-5xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Audio editing is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen was only a generic Coming Soon placeholder. No audio catalog, upload intake, project store, waveform or timeline engine, effects processor, render worker, export service, rights model, signed delivery, or audit contract was connected. No audio should be inferred as uploaded, edited, saved, rendered, licensed, exported, or delivered from this page.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><AudioLines aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Editor readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production audio editor requires validated media intake, MIME and size controls, malware scanning, immutable source versions, timeline and effect semantics, deterministic rendering, cancellation and retry states, rights and consent evidence, storage isolation, signed delivery, retention and deletion, and auditable server-side records. None are available through this route.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link href="/media-gallery"><Button>View media status</Button></Link><Link href="/asset-management"><Button variant="outline">View asset status</Button></Link><Link href="/privacy"><Button variant="ghost">View privacy status</Button></Link></div></Card><section aria-labelledby="audio-editor-evidence-heading"><h2 id="audio-editor-evidence-heading" className="mb-4 text-xl font-semibold">Current editor evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not enter passwords, access tokens, private keys, seed phrases, storage credentials, confidential recordings, unreleased creative work, licensing documents, or sensitive personal information here. An unavailable editor is not evidence that audio is stored, edited, licensed, rendered, exported, or recoverable.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><FileAudio aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No upload, project creation, timeline edit, effect, waveform analysis, transcription, render, export, download, notification, external share, or media-processing API call is read, written, sent, stored, or simulated by this page.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Download aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No file count, duration, format, render time, processing status, quality result, license, royalty, delivery URL, or audit event is fabricated as a fallback. Verify future media and rights records through independently trusted server-side systems.</p></div></Card></main></div>;
}
