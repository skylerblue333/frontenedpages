import { Activity, Bot, Database, FileClock, LockKeyhole, Network, ShieldAlert, Workflow } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const evidence = [
  { label: "Workflow definitions and ownership", value: "Unavailable", icon: Workflow },
  { label: "Triggers, conditions, versions, and approvals", value: "Not configured", icon: FileClock },
  { label: "Actions, integrations, credentials, and permissions", value: "Not connected", icon: Network },
  { label: "Runs, outcomes, retries, and observability", value: "Disabled", icon: Activity },
];

export default function AutomationWorkflows() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Workflow} title="Automation Workflows" subtitle="Workflow services are not connected in this deployment. No workflow, trigger, condition, action, run, template, integration, credential, or execution result is being reported or changed." /><main className="mx-auto max-w-5xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Workflow automation is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen exposed fabricated workflows for premium-content unlocks, subscriber messages, engagement boosts, fraud freezes, SKY staking rewards, AI nudges, PPV purchases, moderation, charity announcements, and wallet top-up prompts. It also claimed 7/10 active workflows, 15,847 total runs, 99.4% success, and 120ms latency. No event source, workflow registry, action provider, payment or wallet connection, AI provider, moderation service, notification channel, credential store, approval model, queue, or audit contract was connected. Those workflows, templates, metrics, switches, install controls, and Builder actions were removed rather than implying that any side effect occurred.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Workflow aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Workflow readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production workflow builder requires typed event schemas, authenticated ownership, deterministic conditions, versioning, least-privilege credentials, allowlisted actions, approval gates, idempotency, durable queues, retry and dead-letter handling, timeout and cancellation semantics, side-effect receipts, secret protection, privacy filtering, monitoring, and independently verifiable audit records. None are available through this route.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link href="/automation-engine"><Button>View engine status</Button></Link><Link href="/automation-rules"><Button variant="outline">View rule status</Button></Link><Link href="/api-integrations"><Button variant="ghost">View integration status</Button></Link></div></Card><section aria-labelledby="workflow-evidence-heading"><h2 id="workflow-evidence-heading" className="mb-4 text-xl font-semibold">Current workflow evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not enter passwords, access tokens, private keys, seed phrases, API credentials, payment data, wallet information, confidential workflows, or sensitive personal information here. An unavailable workflow page is not evidence that an action ran, was safe, was authorized, or was reversible.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Database aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No workflow query, template install, rule toggle, builder action, payment, wallet transfer, AI call, moderation decision, message, notification, file operation, webhook, or external integration call is read, written, sent, stored, or simulated by this page.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Bot aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No workflow count, active state, run count, success rate, latency, engagement total, fraud score, staking result, reward projection, balance, AI output, moderation result, donor notification, or execution outcome is fabricated as a fallback. Verify future automation records through independently trusted server-side systems.</p></div></Card></main></div>;
}
