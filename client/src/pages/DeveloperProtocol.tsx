import { AlertTriangle, CheckCircle2, Code2, FileCheck2, GitBranch, KeyRound, LockKeyhole, Network, ShieldAlert, Webhook } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const readiness = [
  { label: "Authenticated API inventory, contracts, versions, environments, and compatibility policy", value: "Not connected", icon: Network },
  { label: "Authorization, scopes, validation, rate limits, idempotency, errors, and audit", value: "Unavailable", icon: KeyRound },
  { label: "Webhook provenance, signatures, retries, delivery state, SDKs, and documentation", value: "Not configured", icon: Webhook },
  { label: "Security, privacy, data minimization, support, partner terms, and compliance evidence", value: "Not verified", icon: LockKeyhole },
];

const boundaries = [
  { title: "No API or endpoint claim", description: "No route, method, schema, response, user, social, token, payment, NFT, AI, staking, marketplace, analytics, webhook event, SDK, package, version, region, uptime, latency, or compatibility result is fetched, displayed, calculated, or simulated.", icon: Network },
  { title: "No integration or credential action", description: "No API key, OAuth flow, JWT, webhook registration, signature verification, endpoint call, SDK install, clipboard copy, partner application, payment, wallet, account, API request, database read or write, or secret operation can be initiated here.", icon: KeyRound },
  { title: "No security, rate-limit, or compliance claim", description: "No authentication guarantee, scope, rate limit, retry policy, SLA, SOC 2 status, encryption result, privacy outcome, data-processing term, support commitment, partner revenue share, or compliance result is asserted.", icon: FileCheck2 },
  { title: "API integration and secret-safety warn-and-proceed", description: "API credentials and webhooks can expose personal data, financial data, wallets, payments, code, and production systems. Verify the official contract, least-privilege scopes, validation, idempotency, rate limits, signature handling, secret storage, data processing, and rollback before integrating.", icon: ShieldAlert },
];

export default function DeveloperProtocol() {
  return <div className="min-h-screen bg-background"><PageHeader backHref="/developer-area" icon={Code2} title="Developer Protocol" subtitle="API-governance readiness status; no live API, webhook, SDK, partner, authentication, rate-limit, data, payment, wallet, or production integration is available in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><AlertTriangle aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Developer protocol is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen claimed a live API, complete REST and WebSocket coverage, OAuth/JWT/API-key authentication, a 99.9% uptime SLA, latency and region metrics, SOC 2 compliance, endpoints for user, token, payment, NFT, AI, staking, marketplace and admin analytics data, webhook delivery, rate limits, official SDK packages, clipboard installs, and a 20% partner revenue share. No verified API contract, backend, security, compliance, package, partner, or support integration supported those claims, so they were removed.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Code2 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">API-governance readiness status</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">A trustworthy developer protocol requires versioned contracts, authenticated environments, least-privilege authorization, strict input and output validation, safe error handling, idempotency, rate limiting, signed webhooks, provenance, SDK testing, secret isolation, privacy, auditability, support, and change management. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{readiness.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="protocol-boundaries-heading"><h2 id="protocol-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2">{boundaries.map(({ title, description, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><Icon aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{description}</p></Card>)}</div></section><div className="flex flex-wrap gap-3"><Link href="/developer-area"><Button variant="outline">View developer status</Button></Link><Link href="/security-center"><Button variant="outline"><LockKeyhole aria-hidden="true" className="mr-2 h-4 w-4" />View security status</Button></Link><Link href="/api-documentation"><Button variant="outline"><FileCheck2 aria-hidden="true" className="mr-2 h-4 w-4" />View documentation status</Button></Link><Link href="/contact-us-form"><Button variant="outline"><GitBranch aria-hidden="true" className="mr-2 h-4 w-4" />Ask about integration availability</Button></Link></div><Card className="border border-border/50 bg-card p-6"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No route, method, schema, response, user, social, token, payment, NFT, AI, staking, marketplace, analytics, webhook event, SDK, package, version, region, uptime, latency, compatibility, API key, OAuth flow, JWT, webhook registration, endpoint call, SDK install, clipboard copy, partner application, payment, wallet, API request, database read or write, authentication guarantee, rate limit, SLA, compliance result, or partner revenue share is performed. This page is not evidence of API availability, security, privacy, reliability, compliance, or integration support.</p></div></Card></main></div>;
}
