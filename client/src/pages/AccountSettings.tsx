import { Bell, Eye, LockKeyhole, Mail, ShieldAlert, ShieldCheck, Trash2, UserCog } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";
import { useAuth } from "@/_core/hooks/useAuth";

const states = [
  { label: "Account identity and email verification", value: "Unavailable", icon: Mail },
  { label: "Password, sessions, and two-factor security", value: "Not connected", icon: LockKeyhole },
  { label: "Notification preferences and delivery", value: "Not persisted", icon: Bell },
  { label: "Deletion, export, and retention controls", value: "Disabled", icon: Trash2 },
];

export function AccountSettings() {
  const { isAuthenticated } = useAuth();
  const signIn = () => { window.location.href = "/api/oauth/login"; };
  return <div className="min-h-screen bg-background"><PageHeader icon={UserCog} title="Account Settings" subtitle="Account, security, notification, and deletion services are not connected in this deployment. No identity, verification, preference, credential, or destructive result is being reported." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8">{!isAuthenticated && <Card className="border border-primary/30 bg-primary/5 p-6"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-primary" /><div><h2 className="font-semibold">Sign in to check account access</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">Account settings require a verified authenticated session. Signing in does not imply that account records, security controls, notification preferences, or deletion workflows are connected.</p><Button className="mt-4" onClick={signIn}>Sign in securely</Button></div></div></Card>}<Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Account mutations are unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen read email from localStorage, claimed the email was verified, displayed password and two-factor controls, toggled notification preferences with local state and fake success toasts, and “deleted” an account by clearing browser storage. No server-side identity, credential, session, notification, consent, export, retention, or deletion contract was connected. Those claims and mutations were removed. This route cannot change passwords, enable 2FA, save notifications, or delete an account.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><UserCog aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Account-settings readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production account-settings service requires server-side identity and authorization, verified email and recovery flows, password hashing and reset, session invalidation, MFA enrollment and recovery, notification consent and delivery, data export and deletion, retention policy, audit records, CSRF protection, rate limits, and clear success and failure states. None are available through this route.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{states.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-4"><Link href="/security"><Button>View security status</Button></Link><Link href="/privacy"><Button variant="outline">View privacy status</Button></Link><Link href="/contact-us-form"><Button variant="ghost">Ask about account access</Button></Link></div></Card><section aria-labelledby="account-state-heading"><h2 id="account-state-heading" className="mb-4 text-xl font-semibold">Current account evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{states.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Eye aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not treat a signed-out or unavailable settings page as evidence of account ownership, email verification, password security, MFA, notification consent, deletion, export, or retention. Never enter passwords, access tokens, private keys, seed phrases, or sensitive personal data into an unverified client-side form.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><ShieldCheck aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Use the provider’s official account-security workflow until reviewed server-side account controls are connected and independently tested.</p></div></Card></main></div>;
}

export default AccountSettings;
