import { AlertTriangle, CheckCircle2, FileCheck2, KeyRound, Link2, LockKeyhole, ShieldAlert, UserRound } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const boundaries = [
  { label: "Authenticated administrator, workspace, provider, tenant, and authorization scope", value: "Not connected", icon: KeyRound },
  { label: "Provider identity, client registration, redirect URI, scope, state, nonce, and consent provenance", value: "Unavailable", icon: FileCheck2 },
  { label: "Account linking, callback, token exchange, rotation, revocation, and user-control behavior", value: "Not verified", icon: Link2 },
  { label: "Secrets, tokens, personal data, retention, privacy, security, and safeguards", value: "Not configured", icon: LockKeyhole },
];

const surfaces = [
  { title: "Administrator, tenant, and provider scope", scope: "Authenticated administrator, workspace, provider, tenant, role, purpose, consent, and authorization", status: "Unavailable", icon: KeyRound },
  { title: "OAuth registration and callback provenance", scope: "Provider identity, client registration, redirect URI, state, nonce, PKCE, scope, consent, callback, and timestamp", status: "Not connected", icon: FileCheck2 },
  { title: "Account linking and credential lifecycle", scope: "Account linking, token exchange, encrypted custody, rotation, revocation, unlinking, error handling, and user-control behavior", status: "Not verified", icon: Link2 },
  { title: "Secrets, privacy, and access controls", scope: "Client secrets, access tokens, refresh tokens, personal data, retention, deletion, privacy, security, and access", status: "Not configured", icon: ShieldAlert },
];

export default function OAuthProviders() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Link2} title="OAuth Providers" subtitle="External-identity readiness status; no authenticated administrator, provider registry, OAuth client configuration, callback handler, token vault, account-linking service, or production identity backend is connected in this deployment." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-amber-400/30 bg-amber-950/20 p-6"><div className="flex items-start gap-3"><AlertTriangle aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-amber-300" /><div><h2 className="font-semibold text-amber-100">OAuth provider configuration is unavailable</h2><p className="mt-1 text-sm leading-6 text-amber-100/80">The previous screen was an authenticated-only shell with inert Sign In, New, search, settings, and loading controls but did not connect providers, client registrations, redirect URIs, state or nonce validation, PKCE, scopes, consent, account linking, token exchange, token custody, rotation, revocation, privacy, or authorization. It was replaced with this explicit readiness boundary. No provider, client, callback, account link, token, secret, consent state, or availability state is displayed, queried, stored, exchanged, revoked, or mutated from this page.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Link2 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">External-identity readiness boundary</h2><p className="mt-2 max-w-4xl text-sm leading-6 text-muted-foreground">Trustworthy OAuth integration requires server-side provider registration, exact redirect URI allowlists, state and nonce validation, PKCE, least-privilege scopes, explicit consent, secure token exchange and encrypted custody, rotation and revocation, account-linking protections, audit evidence, clear failure states, and least-privilege administration. A provider, linked account, token, consent state, or authentication success is not a fact without verified server-side records. None are connected through this page.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{boundaries.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div></Card><section aria-labelledby="oauth-surfaces-heading"><h2 id="oauth-surfaces-heading" className="mb-4 text-xl font-semibold">OAuth control surfaces</h2><div className="grid gap-4 md:grid-cols-2">{surfaces.map(({ title, scope, status, icon: Icon }) => <Card key={title} className="border border-border/50 bg-card p-6"><div className="flex items-start justify-between gap-4"><Icon aria-hidden="true" className="h-7 w-7 shrink-0 text-primary" /><span className="rounded-full border border-border/60 px-2 py-1 text-xs text-muted-foreground">{status}</span></div><h3 className="mt-4 text-lg font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{scope}. No provider, client, callback, account link, token, secret, consent, privacy, security, safety, or production status is asserted.</p></Card>)}</div></section><section aria-labelledby="oauth-boundaries-heading"><h2 id="oauth-boundaries-heading" className="mb-4 text-xl font-semibold">Current boundaries</h2><div className="grid gap-4 md:grid-cols-2"><Card className="border border-border/50 bg-card p-6"><CheckCircle2 aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">No OAuth operation</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">No auth check, provider or client lookup, redirect or callback, state or nonce validation, PKCE exchange, scope grant, account link, token read or write, revocation, API request, database read or write, export, or identity operation is performed.</p></Card><Card className="border border-border/50 bg-card p-6"><AlertTriangle aria-hidden="true" className="mb-4 h-7 w-7 text-primary" /><h3 className="text-lg font-semibold">Secrets, personal data, privacy, safety, accessibility, security, consent, and authorization warn-and-proceed</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Do not enter client secrets, access tokens, refresh tokens, passwords, authentication codes, identity documents, private keys, health or legal records, financial information, or confidential data here. Do not treat this page as evidence of provider availability, account linking, authentication success, consent, token security, revocation, privacy protection, or security certification. Verify provider identity, redirect allowlist, state, nonce, PKCE, scopes, consent, token custody, rotation, revocation, retention, accessibility, privacy, security, and authorization before configuring an OAuth integration.</p></Card></div></section><div className="flex flex-wrap gap-3"><Link href="/security"><Button variant="outline"><ShieldAlert aria-hidden="true" className="mr-2 h-4 w-4" />Review security</Button></Link><Link href="/privacy-center"><Button variant="outline"><LockKeyhole aria-hidden="true" className="mr-2 h-4 w-4" />Review privacy</Button></Link><Link href="/profile"><Button variant="outline"><UserRound aria-hidden="true" className="mr-2 h-4 w-4" />Review account status</Button></Link></div><Card className="border border-border/50 bg-card p-6"><p className="text-sm leading-6 text-muted-foreground">No auth check, provider or client lookup, redirect or callback, state or nonce validation, PKCE exchange, scope grant, account link, token read or write, revocation, API request, database read or write, export, or identity operation is performed. This page is not evidence of provider availability, account linking, authentication success, consent, token security, revocation, privacy protection, or security certification.</p></Card></main></div>;
}
