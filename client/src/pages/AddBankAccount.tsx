import { AlertTriangle, Banknote, CheckCircle2, CreditCard, LockKeyhole, ShieldCheck, WalletCards } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const states = [
  { label: "Bank provider and secure link", value: "Not connected", icon: Banknote },
  { label: "Identity and account ownership", value: "Unavailable", icon: ShieldCheck },
  { label: "Balances, transfers, and settlement", value: "Not reported", icon: WalletCards },
  { label: "Financial data storage and deletion", value: "Disabled", icon: LockKeyhole },
];

export default function AddBankAccount() {
  return <div className="min-h-screen bg-background"><PageHeader icon={Banknote} title="Add a Bank Account" subtitle="Bank-account linking is not available in this deployment. No bank, owner, balance, transfer, payment method, or verification result is being reported or changed." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><AlertTriangle aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Bank linking is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen was a generic placeholder with an inert Activate control. No bank aggregator, secure credential handoff, account-ownership verification, consent flow, payment processor, balance source, transfer rail, fraud control, or financial-data deletion contract was connected. This route does not collect banking credentials or simulate activation.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><Banknote aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Secure bank-link readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">A production bank-linking flow requires a regulated provider, server-side credential exchange, explicit consent, identity and ownership verification, account and routing-token protection, least-privilege access, masking, webhook reconciliation, transaction and balance semantics, fraud controls, audit records, retention and deletion, and clear failure and revocation states. None are available through this screen.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{states.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-4"><Link href="/wallet"><Button>View wallet status</Button></Link><Link href="/payment-methods"><Button variant="outline"><CreditCard className="mr-2 h-4 w-4" />View payment status</Button></Link><Link href="/contact-us-form"><Button variant="ghost">Ask about bank access</Button></Link></div></Card><section aria-labelledby="bank-state-heading"><h2 id="bank-state-heading" className="mb-4 text-xl font-semibold">Current bank-link evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{states.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Never enter bank usernames, passwords, one-time codes, full account numbers, routing numbers, card numbers, seed phrases, private keys, or payment credentials into this page. Do not treat this screen as proof of account ownership, verification, balance, transfer, settlement, or payout capability.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><CheckCircle2 aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Use your bank or a verified regulated provider’s official secure flow until reviewed financial integrations are connected and independently tested.</p></div></Card></main></div>;
}
