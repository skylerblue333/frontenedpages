import { Activity, BarChart3, Database, Gauge, LockKeyhole, Network, ServerCrash, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { PageHeader } from "@/components/PageHeader";

const evidence = [
  { label: "API latency, throughput, and error rate", value: "Not measured", icon: Gauge },
  { label: "Database queries, pool, and replication", value: "Not connected", icon: Database },
  { label: "Cache, CPU, memory, and disk telemetry", value: "Unavailable", icon: Activity },
  { label: "Engine health, uptime, and incident history", value: "Not configured", icon: Network },
];

export default function AnalyticsDashboard() {
  return <div className="min-h-screen bg-background"><PageHeader icon={BarChart3} title="Ecosystem Analytics" subtitle="Observability services are not connected in this deployment. No API, database, cache, resource, engine-health, uptime, error, or performance result is being reported." /><main className="mx-auto max-w-6xl space-y-8 px-4 py-8"><Card className="border border-red-400/30 bg-red-950/20 p-6"><div className="flex items-start gap-3"><ShieldAlert aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-red-300" /><div><h2 className="font-semibold text-red-100">Ecosystem telemetry is unavailable</h2><p className="mt-1 text-sm leading-6 text-red-100/80">The previous screen displayed fabricated real-time monitoring for 10 strategic engines, including 145ms API p95, 0.02% error rate, 87% cache hit rate, 99.2% uptime, database query and pool values, CPU/memory/disk charts, request and error totals, latency, throughput, and error distribution. No metrics pipeline, infrastructure provider, database telemetry, cache provider, engine registry, time-series source, or incident contract was connected. Those charts, targets, tables, and status badges were removed rather than implying production observability.</p></div></div></Card><Card className="border border-primary/20 bg-gradient-to-br from-primary/10 to-secondary/10 p-8"><div className="flex items-start gap-4"><div className="rounded-xl bg-primary/15 p-3"><BarChart3 aria-hidden="true" className="h-8 w-8 text-primary" /></div><div><h2 className="text-3xl font-bold">Observability readiness</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">Production observability requires authoritative telemetry sources, metric and event contracts, units and aggregation semantics, time-window and timezone definitions, sampling and freshness, service identity, access control, privacy filtering, alert linkage, retention, redaction, failure states, and independently auditable records. None are available through this screen.</p></div></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-primary/30 bg-background/80 p-4"><Icon aria-hidden="true" className="mb-3 h-7 w-7 text-primary" /><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-semibold">{value}</p></Card>)}</div><div className="mt-6 flex flex-wrap gap-3"><Link href="/api-monitoring"><Button>View API monitoring status</Button></Link><Link href="/api-status"><Button variant="outline">View API status</Button></Link><Link href="/alert-configuration"><Button variant="ghost">View alerting status</Button></Link></div></Card><section aria-labelledby="telemetry-evidence-heading"><h2 id="telemetry-evidence-heading" className="mb-4 text-xl font-semibold">Current telemetry evidence</h2><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">{evidence.map(({ label, value, icon: Icon }) => <Card key={label} className="border border-border/50 bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-center gap-2"><Icon aria-hidden="true" className="h-4 w-4 text-muted-foreground" /><p className="font-semibold">{value}</p></div></Card>)}</div></section><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><LockKeyhole aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">Do not enter passwords, access tokens, private keys, seed phrases, database credentials, infrastructure secrets, confidential incident data, or sensitive personal information here. An unavailable analytics dashboard is not evidence of uptime, safety, reliability, performance, capacity, or incident response.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><ServerCrash aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No chart refresh, time-range query, telemetry read, database query, cache read, engine-health probe, alert, export, report, notification, or external observability call is initiated by this page.</p></div></Card><Card className="border border-border/50 bg-card p-5"><div className="flex items-start gap-3"><Gauge aria-hidden="true" className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground" /><p className="text-sm leading-6 text-muted-foreground">No response time, error rate, cache ratio, uptime, query time, connection pool, replication lag, CPU, memory, disk, request, error, throughput, engine status, target, or incident metric is fabricated as a fallback. Verify future telemetry through independently trusted server-side records.</p></div></Card></main></div>;
}
